export enum CurrentMode {
    "Dark" = "dark",
    "Light" = "light"
}

export enum CurrentPage {
    "Home" = "home",
    "Favorites" = "favorites"
}

export enum CurrentTemp {
    "Celcius" = "C",
    "Fahrenheit" = "F"
}